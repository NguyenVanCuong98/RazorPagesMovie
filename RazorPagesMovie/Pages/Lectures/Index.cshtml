@page
@model RazorPagesMovie.Pages.Lectures.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lecture[0].Lecture_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Lecture) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lecture_Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Lecture_ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Lecture_ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Lecture_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
